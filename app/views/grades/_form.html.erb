<br>
<div class="row">
  <div class="col xl12 l12 m12 s12">

    <%= form_with(model: grade, local: true) do |form| %>
      <% if grade.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(grade.errors.count, "error") %> prohibited this grade from being saved:</h2>

          <ul>
            <% grade.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <!--  <div class="field">-->
      <%#= form.label :alumno_id %>
      <%#= form.text_field :alumno_id, id: :grade_alumno_id %>
      <!--  </div>-->

      <div class="row">
        <div class="col xl6 l6 m12 s12">
          <div class="input">
            <%= form.label :alumno_id %>
            <%= form.collection_select :alumno_id, Alumno.all, :id, :nombre, {prompt: "Seleccione un Alumno"}, class: "browser-default" %>
          </div>
        </div>
        <div class="col xl6 l6 m12 s12">
          <div class="input">
            <%= form.label :periodo_id %>
            <%= form.collection_select :periodo_id, Periodo.all, :id, :nombre, {prompt: "Seleccione un Periodo"}, class: "browser-default" %>
          </div>
        </div>
      </div>

      <div class="row">
        <div class="col xl6 l6 m12 s12">
          <div class="input">
            <%= form.label 'Semestre'%>
            <%= form.collection_select :record_id, Record.all, :id, :id, {prompt: "Seleccione un Semestre"}, class: "browser-default" %>
          </div>
        </div>
        <div class="col xl6 l6 m12 s12">
          <div class="input">
            <%= form.label :curso_id %>
            <%= form.collection_select :curso_id, Curso.all, :id, :id, {prompt: "Seleccione una Inscripción"}, class: "browser-default" %>
          </div>
        </div>
      </div>

      <div class="row">
        <div class="col xl3 l3 m12 s12">
          <div class="input">
            <%= form.label :parcial1 %>
            <%= form.text_field :parcial1, id: :grade_parcial1 %>
          </div>
        </div>

        <div class="col xl3 l3 m12 s12">
          <div class="input">
            <%= form.label :parcial2 %>
            <%= form.text_field :parcial2, id: :grade_parcial2 %>
          </div>
        </div>

        <div class="col xl3 l3 m12 s12">
          <div class="input">
            <%= form.label 'Promedio' %>
            <%= form.text_field :average, id: :grade_average %>
          </div>
        </div>
      </div>

      <div class="row">
        <div class="col xl3 l3 m12 s12">
          <div class="input">
            <%= form.label 'Extraordinario 1' %>
            <%= form.text_field :extra1, id: :grade_extra1 %>
          </div>
        </div>

        <div class="col xl3 l3 m12 s12">
          <div class="input">
            <%= form.label 'Extraordinario 2' %>
            <%= form.text_field :extra2, id: :grade_extra2 %>
          </div>
        </div>
      </div>
      <div class="row">
        <div class="col xl12 l12 m12 s12 right-align">
          <div class="actions " style="z-index: 2;">
            <%= form.submit 'Aceptar', :class => 'waves-effect waves-teal btn' %>
          </div>
        </div>
      </div>

    <% end %>
  </div>
</div>

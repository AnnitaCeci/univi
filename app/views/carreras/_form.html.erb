<br>
<div class="row">
<div class="col xl12 l12 m12 s12">


<%= form_for(carrera) do |f| %>
  <% if carrera.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carrera.errors.count, "error") %> prohibited this carrera from being saved:</h2>

      <ul>
        <% carrera.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div class="row">
      <div class="col xl6 l6 m12 s12">
        <div class="input">
          <%= f.label :Nombre %>
          <%= f.text_field :nombre %>
        </div>
      </div>
      <div class="col xl6 l6 m12 s12">
        <div class="input">
          <%= f.label :clave %>
          <%= f.text_field :clave %>
        </div>
      </div>
 </div>

<div class="row">
  <div class="col xl12 l12 m12 s12">
    <div class="actions">
      <%= f.submit 'Aceptar', :class => 'waves-effect waves-teal btn boton-aceptar' %>
    </div>
  </div>
</div>
<% end %>
</div>
</div>